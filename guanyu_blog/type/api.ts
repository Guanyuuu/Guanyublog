export interface debounceTy {
    time:number;
    ():void
}

export interface indexProps {
    res:[]
    topD:[]
    botD:[]
}

export interface styleType {
    transform:string
    transition:string
}

export interface op {
    isSearch:boolean,
    keyWord:string
}